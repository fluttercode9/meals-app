import 'package:flutter/material.dart';

class FavoritesScreen extends StatelessWidget {
  static const route = '/favourites';
  @override
  Widget build(BuildContext context) {
    return Container(child: Text('placeholder favs'),
      
    );
  }
}